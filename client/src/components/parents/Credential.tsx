import React from "react";
import FormBody from "../children/FormBody";
import Header from "../children/Header";

const Credential = () => {
  return (
    <div className={"credential"}>
      <Header />
	  <FormBody/>
    </div>
  );
};

export default Credential;
